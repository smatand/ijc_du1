//#include <stdio.h>
//#include <stdlib.h>
//#include "bitset.h"
//
//int main(void) {
//	bitset_create(roga, MAX)
//	for (bitset_index_t i=0; i<MAX; i++)
//		(i % 10 == 0) ? printf("%lu\n", roga[i]) : printf("%lu ", roga[i]);
//
//	printf("\nbitset_alloc\n");
//
//	bitset_alloc(loga, MAX)
//	for (bitset_index_t i=0; i<MAX; i++)
//		(i % 10 == 0) ? printf("%lu\n", loga[i]) : printf("%lu ", loga[i]);
//
//
//	bitset_setbit(loga, 1, 1);
//	printf("\nSize of loga is %d and on index is the value: %d\n", bitset_size(loga), bitset_getbit(loga, 1));
//	bitset_free(loga)
//
//	return 0;
//}